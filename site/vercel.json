{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "hugo --gc --minify",
  "devCommand": "hugo server -D",
  "installCommand": "yum install -y golang && curl -L https://github.com/gohugoio/hugo/releases/download/v0.153.1/hugo_extended_0.153.1_linux-amd64.tar.gz | tar -xz hugo && mv hugo /usr/local/bin/ && hugo mod tidy",
  "outputDirectory": "public",
  "framework": null,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
